{
  "name": "modellink",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun scripts/build-catalog.ts",
    "build:data": "bun scripts/build-data-package.ts",
    "validate": "bun packages/core/script/validate.ts",
    "verify:data": "bun scripts/verify-data-package.ts",
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "check": "bun run typecheck && bun run validate && bun test && bun run build"
  },
  "dependencies": {
    "remeda": "^2.33.7",
    "zod": "3.24.2"
  },
  "devDependencies": {
    "@tsconfig/bun": "^1.0.8",
    "@types/bun": "1.3.0",
    "typescript": "5.8.2"
  }
}
